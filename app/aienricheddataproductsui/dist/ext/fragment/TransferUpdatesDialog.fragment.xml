<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Dialog
        id="transferUpdatesDialog"
        title="Transfer Changes to ERP"
        contentWidth="52rem"
        contentHeight="32rem"
        resizable="true">

        <content>

            <MessageStrip
                id="transferHint"
                text="The product group will be written to the SAP system for the following products. Individual entries can be deselected before transfer."
                type="Information"
                showIcon="true"
                class="sapUiSmallMargin"/>

            <Table
                id="pendingTable"
                items="{pending>/rows}"
                mode="MultiSelect"
                growing="true"
                growingThreshold="50"
                sticky="ColumnHeaders">

                <headerToolbar>
                    <OverflowToolbar id="pendingToolbar">
                        <Title
                            id="pendingTitle"
                            text="Open Changes ({= ${pending>/rows}.length })"/>
                    </OverflowToolbar>
                </headerToolbar>

                <columns>
                    <Column id="colProduct" width="10rem">
                        <Text id="colProductLabel" text="Product"/>
                    </Column>
                    <Column id="colDescription" minScreenWidth="Tablet" demandPopin="true">
                        <Text id="colDescriptionLabel" text="Description"/>
                    </Column>
                    <Column id="colNewGroup" width="9rem" hAlign="Center">
                        <Text id="colNewGroupLabel" text="New Product Group"/>
                    </Column>
                    <Column id="colSource" width="8rem" hAlign="Center" minScreenWidth="Tablet" demandPopin="true">
                        <Text id="colSourceLabel" text="Source"/>
                    </Column>
                    <Column id="colSynced" width="9rem" hAlign="Center" minScreenWidth="Tablet" demandPopin="true">
                        <Text id="colSyncedLabel" text="Current ERP Value"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem id="pendingRow" selected="{pending>selected}">
                        <cells>
                            <ObjectIdentifier id="cellProduct" title="{pending>Product}"/>
                            <Text id="cellDescription" text="{pending>ProductDescription}"/>
                            <ObjectStatus id="cellNewGroup" text="{pending>ProductGroup}" state="Success"/>
                            <Text id="cellSource" text="{pending>ProductGroupSource}"/>
                            <Text id="cellSynced" text="{= ${pending>SyncedProductGroup} || '–' }"/>
                        </cells>
                    </ColumnListItem>
                </items>

            </Table>

        </content>

        <beginButton>
            <Button
                id="confirmTransferButton"
                text="Transfer"
                type="Emphasized"
                press=".onConfirmTransfer"/>
        </beginButton>

        <endButton>
            <Button
                id="cancelTransferButton"
                text="Cancel"
                press=".onCancelTransfer"/>
        </endButton>

    </Dialog>

</core:FragmentDefinition>
